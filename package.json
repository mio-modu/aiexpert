{
  "name": "ai-presentation",
  "version": "1.0.0",
  "description": "Modern AI-powered presentation layout",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r *.html *.js *.css dist/",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "http": "^0.0.1-security",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
} 